<section class="row">
	<h2 class="a-small components__title">Trigger scripts by css-mediaqueries. Bento-jQuery-plugin: <code>getActiveMQ();</code></h2>
	<p>Functions are triggered by your screen-states (alpha, beta, charlie, delta, echo) defined in <code>settings.scss</code>. The plugin uses font-families, defined in src/sass/states/_states.scss -> no need to edit though.</p>
    <p>Usage: See <code>../src/js/scripts.js</code></p>
	<p><strong id="js-mq-example">Placeholder—no functions are triggered. Resize window to alpha...</strong></p>
	<p>
		aq = alphaquery<br/>
		bq = betaquery<br/>
		cq = charliequery<br/>
		dq = deltaquery<br/>
		eq = echoquery<br/>
	</p>
	<p><strong id="js-mq-currentexample"></strong></p>
</section>